version: "2"

services:
  postgres:
    image: postgres
    networks:
      - "towan"

  towan:
    restart: always
    build: ./app
    container_name: towan
    ports:
      - "8000:8000"
    depends_on:
      - postgres
    volumes:
      - /usr/src/app
    environment:
      - PRODUCTION=True
    labels:
      - "traefik.enable=false"
    networks:
      - "towan"
    #command: /usr/local/bin/gunicorn towan.wsgi:application -w 2 -b :8000 --reload

  nginx:
    restart: always
    image: nginx
    volumes_from:
      - towan
    depends_on:
      - towan
    volumes:
      - "/usr/default.conf:/etc/nginx/conf.d/default.conf:ro"
    labels:
      - "traefik.enable=true"
      - "traefik.backend=nginx"
      - "traefik.frontend.rule=Host:lola.ninja"
      #- "traefik.frontend.passHostHeader=true"
      - "traefik.port=80"
      - "traefik.docker.network=reverseproxy_default"
    links:
      - towan
    networks:
      - "reverseproxy_default"
      - "towan"

  whoami:
    image: emilevauge/whoami
    labels:
      - "traefik.enable=true"
      - "traefik.backend=whoami"
      - "traefik.frontend.rule=Host:lola.ninja"
      - "traefik.frontend.rule=PathPrefix:/whoami/"
      - "traefik.docker.network=reverseproxy_default"
    networks:
      - "reverseproxy_default"

networks:
  towan:
    driver: bridge
  reverseproxy_default:
    external:
      name: reverseproxy_default
